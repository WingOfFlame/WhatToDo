<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionModeOverlay">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.ButtonOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorButtonNormal">@color/btnDisabled</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.ButtonOverlay.positive" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorAccent">@color/btnPositive</item>
    </style>

    <style name="AppTheme.ButtonOverlay.negative" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorAccent">@color/btnNegative</item>
    </style>

    <style name="AppTheme.ButtonOverlay.yellow" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorAccent">#FFD740</item>
        =
    </style>

    <style name="AppTheme.ButtonOverlay.white" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorAccent">@android:color/white</item>
        <item name="android:textColorPrimaryInverse">@drawable/button_text_color
        </item>

    </style>

    <style name="menu_labels_style">
        <item name="android:background">@drawable/round_rect_shape</item>
        <item name="android:textColor">@color/color_primary_text</item>
    </style>
</resources>
